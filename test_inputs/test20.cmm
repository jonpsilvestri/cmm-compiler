/* codegen: IF statements: expressions containing a single kind of operator */

int main() {
    int x, y;

    if (1+2+3 != 2+2)
        println(1);
    else
        println(0);

    x = 1;
    y = 2;

    if (y*2*3 == x*3*4)
        println(1);
    else
        println(0);
}
